## What

<!-- What does this PR do? Link the issue if one exists. -->

## How was it tested?

<!-- Every feature ships with tests in the same PR. Which tests cover this?
     For console changes: emulator run? real hardware? -->

- [ ] `cargo fmt --all --check` passes
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [ ] `cargo test --workspace` passes
- [ ] New behavior is covered by new tests

## Platform impact

<!-- Which targets does this touch? PC / N64 / 3DS / editor / pipeline.
     Anything that changes the dependency graph or the game-api ABI? -->
